# Upper bounds cap each dependency to the major version this app has been
# verified against. A fresh resolve without them pulls pandas 3.x / reportlab
# 5.x, which are untested here — and unbounded ranges are what let the
# starlette break below reach us in the first place.

streamlit>=1.61.0,<2
# Streamlit 1.61 declares starlette<2,>=0.46.0, but starlette 1.4.0 added a
# required keyword-only `thread_minimum_size` to GZipResponder.__init__ that
# Streamlit's MediaAwareGZipMiddleware does not pass. Any gzip-accepting client
# (i.e. every browser) then gets HTTP 500 on /_stcore/*. Pin below 1.4 until
# Streamlit tightens its own constraint.
starlette<1.4

plotly>=5.18.0,<7
pandas>=2.0.0,<3
openpyxl>=3.1.0,<4
reportlab>=4.0.0,<5
matplotlib>=3.7.0,<4
# Used directly by the PDF chart builder; do not rely on it arriving
# transitively via streamlit/pandas.
numpy>=1.24.0,<3
